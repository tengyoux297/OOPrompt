# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-extension
dist-ssr
*.local

# Environment variables
# .env is committed (contains assistant IDs only, safe to share)
# .env.local contains API keys and is gitignored (never commit this!)
# The *.local pattern above already covers .env.local, but listing explicitly for clarity
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
